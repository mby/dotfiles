# theme
sudo apt install -y materia-gtk-theme gnome-tweaks

# desktop
cat gnome/.interface.dconf | dconf load /org/gnome/desktop/interface/

# terminal
cat gnome/.terminal.dconf | dconf load /org/gnome/terminal/
